services:
  mediapulse:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - mediapulse-data:/app/data
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
    restart: unless-stopped

volumes:
  mediapulse-data:
